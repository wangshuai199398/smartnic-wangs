# libsmartnic 用户态库入口。
# 当前 12.4 的小型控制库和 CLI 位于 tools/，这里转发构建，后续
# 13.x 再把 Verbs provider 代码接入本目录。

.PHONY: all clean

all:
	@echo "[lib/libsmartnic] Verbs provider 暂未实现，构建 12.4 控制工具。"
	@$(MAKE) -C ../../tools

clean:
	@$(MAKE) -C ../../tools clean
